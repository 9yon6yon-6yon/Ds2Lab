#include <bits/stdc++.h>
using namespace std;

int main()
{

    int T;
    while (T > 0)
    {
        int N, M;
        cin >> T;
        cin >> N >> M;

        for (int i = 0; i < M; i++)
        {
        }
        int Q;
        cin >> Q;
        int A, K;
        for (int i = 0; i < Q; i++)
        {
            cin >> A >> K;
        }
        T--;
    }

    return 0;
}